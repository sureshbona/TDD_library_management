"use strict";

const express = require("express");
const router = express.Router();
const controller = require("../controllers/user.controller");

// no routes for user as of now.

module.exports = router;
